
services:
  postgres:
    build: .
    container_name: postgres_for_twin_twitter
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: